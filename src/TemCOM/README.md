# TemCOM

Components specific to the Templates page live here.

Suggested structure:
- `TemplateCard.js` – individual template preview
- `TemplateGrid.js` – responsive grid layout
- `TemplateFilters.js` – filter/search UI
- `TemplateCategoryTabs.js` – category navigation

Add component files as you build out the Templates page UI.
